[target.'cfg(all(target_arch = "arm", target_os = "none"))']
runner = "probe-rs run --chip nRF52840_xxAA"
#runner = "probe-rs run --chip nRF52832_xxAA"
#runner = "probe-rs run --chip nRF54L15_M33"

[build]
# Pick ONE of these compilation targets
# target = "thumbv6m-none-eabi"    # Cortex-M0 and Cortex-M0+
# target = "thumbv7m-none-eabi"    # Cortex-M3
target = "thumbv7em-none-eabi"   # Cortex-M4 and Cortex-M7 (no FPU)
# target = "thumbv7em-none-eabihf" # Cortex-M4F and Cortex-M7F (with FPU)
# target = "thumbv8m.main-none-eabi"   # Cortex-M33 (no FPU) — nRF5340 network core
# target = "thumbv8m.main-none-eabihf" # Cortex-M33F (with FPU) — nRF54L

[env]
DEFMT_LOG = "info"
